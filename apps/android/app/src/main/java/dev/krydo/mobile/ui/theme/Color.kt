package dev.krydo.mobile.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Krydo Stellar visual identity — aligned with krydo-stellar website.
 * Brand accent is Electric/Stellar Blue (not gold).
 */
object KrydoColors {
    // Backgrounds
    val BackgroundPrimary = Color(0xFF090B12)
    val BackgroundSecondary = Color(0xFF0D111B)
    val SurfaceElevated = Color(0xFF111521)
    val CardSurface = Color(0xFF141925)

    // Primary brand
    val ElectricBlue = Color(0xFF2563EB)
    val BrightBlue = Color(0xFF3B82F6)
    val StellarBlue = Color(0xFF2F6BFF)

    // Secondary accents
    val Cyan = Color(0xFF38BDF8)
    val Violet = Color(0xFF6366F1)
    val Purple = Color(0xFF8B5CF6)

    // Text
    val TextPrimary = Color(0xFFF8FAFC)
    val TextSecondary = Color(0xFFCBD5E1)
    val TextMuted = Color(0xFF94A3B8)

    // Borders
    val BorderSubtle = Color(0x2494A3B8) // rgba(148,163,184,0.14)
    val BorderBlue = Color(0x592563EB) // rgba(37,99,235,0.35)

    // Semantic
    val Success = Color(0xFF22C55E)
    val Warning = Color(0xFFF59E0B)
    val Error = Color(0xFFEF4444)

    // Interaction
    val PrimaryPressed = Color(0xFF1D4ED8)
    val GlowBlue = Color(0x402563EB)
}
